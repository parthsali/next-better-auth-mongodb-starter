# Next.js + Better Auth + MongoDB Template

A production-ready template featuring the latest tech stack for modern web applications.

## 🚀 Features

- **Next.js 15** - App Router & Server Actions
- **Better Auth** - Secure authentication (Google & Credentials)
- **MongoDB** - Mongoose ODM for type-safe database interactions
- **Shadcn UI** - Beautiful, accessible components
- **Tailwind CSS** - Utility-first styling
- **Dark Mode** - Built-in theme support
- **TypeScript** - End-to-end type safety

## 🛠️ Getting Started

1. **Clone the repository**

   ```bash
   git clone https://github.com/parthsali/next-better-auth-mongodb-template.git
   ```

2. **Install dependencies**

   ```bash
   pnpm install
   ```

3. **Set up environment variables**

   ```bash
   cp .env.example .env
   ```

   Fill in your generic `BETTER_AUTH_SECRET`, `GOOGLE_CLIENT_ID`, etc.

4. **Run the development server**
   ```bash
   pnpm dev
   ```

Open [http://localhost:3000](http://localhost:3000) with your browser.
